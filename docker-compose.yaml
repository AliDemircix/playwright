version: '3.8'
services:
  playwright:
    image: playwright-test
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run test-runner
    volumes:
      - ./playwright-report/:/app/playwright-report
      - ./test-results/:/app/test-results
